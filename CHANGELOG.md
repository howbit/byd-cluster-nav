# 更新记录

## 2.9.8 — 2026-09-23

- 在两页共用的标题栏右侧加入“GitHub 开源主页”入口，点击后通过系统 HTTPS 处理应用打开项目仓库。
- 浏览器不可用时在应用中显示提示；导航和开机自启逻辑未改动。

## 2.9.7 — 2026-09-23

- 一键配置读回成功后，在界面显示本次结果和检查时间；应用进程重启后显示历史结果与时间，并明确标注本次未复核。
- 无结果或验证失败时显示“尚未读回”，避免把未验证误写为“未配置”。原有的状态异常时自动更正、点击后复核逻辑不变。

## 2.9.6 — 2026-09-23

- 修复副屏保留但地图退出时，新会话缺少初始化记录导致恢复失败的问题。
- 将开机等待 ADB 从广播接收器移入独立的前台服务，限制等待时长，用户关闭时可取消。
- 拒绝在握手完成前确认会话；过早调用 `complete` 不再持锁等待。
- DeviceIdle 与车机设置的成功状态改为读回确认，未知状态不再标记成功。
- 一键配置只允许一个任务和一次完成通知；校验实际开关状态并处理超时。
- 修正状态文案及项目文档，新增真实的配置结果断言和已有通道接管模拟器测试。

## 2.9.5

- 用户报告此版本已在目标车辆上正常开启仪表地图。此前其他版本与实车记录不在本开源源码包内。
