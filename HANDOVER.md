# ClusterNav 2.9.8 技术交接

本文是维护说明。实车成功反馈属于 2.9.5；本地构建和模拟器测试不能替代 2.9.8 实车复测。源代码是命令、超时和状态判定的最终依据。

## 已验证的仪表流程

已成功车辆：DiLink 3.0、厂商 `AutoContainer` 可用、原厂 `com.byd.automap` 的 `MeterActivity` 可用。冷启动顺序是 `sendInfo(1000,16,"")` → 至少 6 秒 → `sendInfo(1000,35,"")` → `auto.setInt(1007,1276157976,4)` → 等待真实 fission Display → 启动/必要时重启原厂地图 → 检测副屏地图窗口和 Surface。关闭为导航属性 `1` 加 `sendInfo(1000,18,"")`，并读回 `auto.getInt(1007,1086337074)` 为 `1`。不要把 container `mode(0)` 与导航属性值 `0` 混为一谈。

`NativeBridge` 由本机授权 ADB 的 shell UID 2000 通过 `CLASSPATH=<实际 APK 路径> app_process` 执行。应用本身不需要 Root。`NativeBridge` 支持 `probe`、`init`、`guard`、`set`、`complete`、`navigate`、`adopt`、`map-start`、`meter-start`、`confirm`、`restore`；不存在 `start`、`stop`、`keepalive` 命令。地图包仅允许代码中明确列出的 OEM 包，不能把通用第三方地图列为已支持。

## 2.9.8 改动

- 共用标题栏右侧加入项目主页入口，固定打开 `https://github.com/howbit/byd-cluster-nav`。该入口不参与仪表导航状态机；车机须有可处理 HTTPS 的应用。

## 2.9.7 改动

- 一键配置仍会检查两个厂商开关；状态不对时尝试更正并读回，状态未知时不盲目切换。没有新增开机时自动打开设置页的行为。
- 设置页状态显示保存的实际读回时间。本次应用进程内读回成功显示“本次”并用绿色；进程重启后显示“上次、本次未复核”并用蓝色。缺少旧版本时间戳时明确显示时间未记录；失败或未知不再写成“未配置”。

## 2.9.6 改动

- `SessionEngine` 的已有副屏分支会执行 `NativeBridge.adopt`。桥接端持锁验证 session 未停止、真实厂商副屏身份及硬件模式匹配，再记录该会话所需的模式、激活和导航状态。错误身份或模式被拒绝；不再直接跳过会话记录调用 `map-start`。显式选择 `container` 与自动选择都能识别同一副屏。
- `confirm` 不再补造尚未完成的握手记录；不足 6 秒的 `complete` 会立即拒绝，避免持锁等待期间用户关闭后仍发送迟到的 `35`。
- `BootReceiver` 只启动 `BootStartupService` 后返回。服务最多等待本机 ADB 两分钟，接受用户关闭或取消自启，不与诊断服务共用生命周期。
- 一键配置检查 DeviceIdle 写入及读回，区分开关开启、关闭、未知。无障碍点击后重新读取；点击失败和状态不明不记成功。单次配置在 UI 与无障碍回调上去重，有总超时并可重试。历史设置状态不以实时绿色显示。
- 文案只声称检测到地图窗口/Surface；仪表真实出图仍需人眼和实车记录。未添加自动重启车机或无限心跳。

## 已完成的本地验证

- 2.9.8 的 `build.py` 已编译并签名 APK，运行 **59 条**纯 Java 断言；与 2.9.7 安装包的签名证书一致，版本号为 2980 / 2.9.8。
- 2.9.6 基线的 `test_guard.py` 曾在 Android 模拟器中通过 **34 条**会话守护断言。
- 2.9.6 基线的 `test_map_restart.py` 曾在模拟器中通过 **15 条**地图与接管断言，包含副屏身份和硬件模式不匹配的拒绝场景。
- 2.9.6 基线的 `test_android.py --boot-only` 曾在模拟器中通过 **3 条**开机广播和独立服务断言。本轮没有连接模拟器，未重新运行这三组集成测试。

纯 Java 断言与先前使用模拟器和单独测试 DEX 的集成测试，都没有验证比亚迪真实权限、无障碍页面布局、开机后的实际车机调度或仪表像素。本轮没有连接模拟器或实车，新增的 GitHub 入口仍需在车机上确认显示和浏览器跳转。2.9.8 的建议实车复测顺序：确认两页标题栏入口并点击；手动开启与关闭；副屏存在时结束地图进程再恢复；完全重启车机后自动启动；关闭自启后的行为；一键配置成功/失败状态及读回时间；重启应用后确认历史标记。每个场景记录应用版本、实际仪表画面和对应诊断 ZIP。

## 维护注意

- 副屏 Display ID 必须从实时探针读取；成功车辆曾出现 Display 1，但不能硬编码。
- `WindowEvidence` 只能证明窗口与 Surface，不能证明物理屏幕上的地图像素。
- 仪表“全屏导航”菜单仍存在，不代表地图正在出图，也不是关闭失败的充分证据。
- `BootStartupService` 与导航会话服务分离；用户主动关闭时要取消仍在等待 ADB 的开机任务。
- 自启动管理页的 ON 表示“禁止后台自启”；应用加速页的 ON 表示“保护开启”。无法读取开关时不要猜测 OFF，更不能直接点选。
- 配置页 AppOps 与 DeviceIdle 是不同状态，历史成功缓存不能替代当前读回。
- 交付公开源码时只使用白名单打包脚本。个人签名密钥、车机日志、逆向参考目录与测试构建缓存不能进入源码包。

开源构建和发布文件清单见 [README.md](README.md) 与 [CHANGELOG.md](CHANGELOG.md)。
